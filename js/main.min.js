/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./app/js/app.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./app/js/app.js":
/*!***********************!*\
  !*** ./app/js/app.js ***!
  \***********************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _utils_helpers__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils/helpers */ "./app/js/utils/helpers.js");
/* harmony import */ var _utils_setViewCurrentIndexSlide__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./utils/setViewCurrentIndexSlide */ "./app/js/utils/setViewCurrentIndexSlide.js");
/* harmony import */ var _modules_initPhotoSwipe__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./modules/initPhotoSwipe */ "./app/js/modules/initPhotoSwipe.js");
/* harmony import */ var _modules_initPhotoSwipe__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_modules_initPhotoSwipe__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _modules_tabs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./modules/tabs */ "./app/js/modules/tabs.js");
/* harmony import */ var _modules_photoGallery__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./modules/photoGallery */ "./app/js/modules/photoGallery.js");
/* harmony import */ var _modules_photoGallery__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(_modules_photoGallery__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var _modules_productQuantity__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./modules/productQuantity */ "./app/js/modules/productQuantity.js");
/* harmony import */ var _modules_productQuantity__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(_modules_productQuantity__WEBPACK_IMPORTED_MODULE_5__);






document.addEventListener('DOMContentLoaded', function () {
  if (document.querySelector('[data-subnav-toggle]')) {
    document.querySelector('[data-subnav-toggle]').addEventListener('click', function (event) {
      var target = event.target;
      var parent = target.closest('.nav__item');

      if (parent) {
        parent.classList.toggle('show');
      }
    });
  }

  if (document.querySelector('#slider-category')) {
    new Splide('#slider-category', {
      perPage: 3,
      gap: 60,
      pagination: false,
      arrowPath: 'M23.5 14L29.5 20M29.5 20L23.5 26M29.5 20H10',
      autoplay: true,
      rewind: true,
      perMove: 1,
      breakpoints: {
        1239: {
          perPage: 3,
          gap: 30
        },
        991: {
          perPage: 2,
          gap: 30
        },
        639: {
          perPage: 1
        }
      }
    }).mount({
      setViewCurrentIndexSlide: _utils_setViewCurrentIndexSlide__WEBPACK_IMPORTED_MODULE_1__["default"]
    });
  }

  if (document.querySelector('#slider-products-new')) {
    new Splide('#slider-products-new', {
      perPage: 4,
      gap: 40,
      pagination: false,
      arrowPath: 'M23.5 14L29.5 20M29.5 20L23.5 26M29.5 20H10',
      arrows: false,
      autoplay: true,
      rewind: true,
      perMove: 1,
      breakpoints: {
        1239: {
          perPage: 4,
          gap: 60
        },
        991: {
          perPage: 3,
          gap: 20
        },
        639: {
          perPage: 2,
          gap: 20
        }
      }
    }).mount();
  }

  if (document.querySelector('#slider-products-similar')) {
    new Splide('#slider-products-similar', {
      perPage: 4,
      gap: 40,
      pagination: false,
      arrowPath: 'M23.5 14L29.5 20M29.5 20L23.5 26M29.5 20H10',
      arrows: true,
      autoplay: true,
      rewind: true,
      perMove: 1,
      breakpoints: {
        1239: {
          perPage: 4,
          gap: 60
        },
        991: {
          perPage: 3,
          gap: 20
        },
        639: {
          perPage: 2,
          gap: 20
        }
      }
    }).mount({
      setViewCurrentIndexSlide: _utils_setViewCurrentIndexSlide__WEBPACK_IMPORTED_MODULE_1__["default"]
    });
  }

  if (document.querySelector('#product-gallery') && document.querySelector('#product-gallery-preview')) {
    var secondarySlider = new Splide('#product-gallery-preview', {
      rewind: true,
      fixedWidth: 80,
      fixedHeight: 80,
      isNavigation: true,
      gap: 20,
      // focus: 'center',
      pagination: false,
      cover: true,
      arrows: false,
      breakpoints: {
        991: {
          fixedWidth: 57,
          fixedHeight: 57,
          gap: 15
        },
        639: {
          destroy: 'completely'
        }
      }
    }).mount();
    var primarySlider = new Splide('#product-gallery', {
      type: 'fade',
      // heightRatio: 0.5,
      pagination: false,
      arrows: false,
      cover: true,
      breakpoints: {
        1239: {
          fixedHeight: 500
        },
        991: {
          fixedHeight: 280
        },
        639: {
          fixedHeight: 280,
          pagination: true
        }
      }
    });
    primarySlider.sync(secondarySlider).mount();
  }

  if (document.querySelector('#map') && document.querySelector('#map-nav')) {
    var mapNavSlider = new Splide('#map-nav', {
      type: 'fade',
      perPage: 1,
      perMove: 1,
      rewind: true,
      isNavigation: true,
      gap: 20,
      focus: 'center',
      pagination: false,
      arrowPath: 'M23.5 14L29.5 20M29.5 20L23.5 26M29.5 20H10',
      arrows: true,
      breakpoints: {
        991: {
          gap: 15
        }
      }
    }).mount({
      setViewCurrentIndexSlide: _utils_setViewCurrentIndexSlide__WEBPACK_IMPORTED_MODULE_1__["default"]
    });
    var mapSlider = new Splide('#map', {
      perMove: 1,
      type: 'fade',
      pagination: false,
      arrows: false,
      breakpoints: {
        639: {
          pagination: false
        }
      }
    });
    mapSlider.sync(mapNavSlider).mount();
  }

  function toggleFilterMore(event) {
    event.preventDefault();
    event.stopPropagation();
    var target = event.target;
    var targetText = target.innerText;
    var parent = target.closest('.filter__part');
    console.log(target.closest('.filter__part'));

    if (parent) {
      var more = parent.querySelector('[data-filter-more]');
      more.toggleAttribute('hidden');
      target.innerText = target.getAttribute('data-text-replace');
      target.setAttribute('data-text-replace', targetText);
    }
  }

  function toggleFilterCollapse(event) {
    event.stopPropagation();
    var target = event.target;
    target.closest('.filter__part').classList.toggle('hide');
  }

  function toggleCatalogDisplay(event) {
    event.stopPropagation();
    var target = event.target;
    var catalog = document.querySelector('[data-catalog-layout]');
    Object(_utils_helpers__WEBPACK_IMPORTED_MODULE_0__["getSiblings"])(target).forEach(function (elem) {
      elem.classList.remove('active');
    });
    target.classList.add('active');

    if (target.classList.contains('catalog-view-list')) {
      catalog.setAttribute('data-catalog-layout', 'list');
    } else if (target.classList.contains('catalog-view-grid')) {
      catalog.setAttribute('data-catalog-layout', 'grid');
    }
  }

  document.querySelectorAll('.filter__more').forEach(function (btn) {
    btn.addEventListener('click', toggleFilterMore);
  });
  document.querySelectorAll('.filter__name').forEach(function (btn) {
    btn.addEventListener('click', toggleFilterCollapse);
  });
  document.querySelectorAll('.catalog-view-grid, .catalog-view-list').forEach(function (btn) {
    btn.addEventListener('click', toggleCatalogDisplay);
  });
  document.querySelector('.mobile-trigger').addEventListener('click', function (event) {
    event.stopPropagation();
    event.preventDefault();
    this.classList.toggle('active');
    document.querySelector('.header__main').classList.toggle('show');
  });

  if (document.querySelector('[data-mobile-filter]')) {
    document.querySelector('[data-mobile-filter]').addEventListener('click', function (event) {
      document.querySelector('.filter').classList.toggle('show');
    });
  }

  var mediaQuery = window.matchMedia('(min-width: 991px)');
  mediaQuery.addListener(movingMobileNav);
  movingMobileNav(mediaQuery);

  function movingMobileNav(event) {
    var search = document.querySelector('.top-panel__search');
    var logo = document.querySelector('.logo');
    var nav = document.querySelector('.nav');
    var topPanel = document.querySelector('.top-panel__inner');

    if (event.matches) {
      topPanel.appendChild(search);
    } else {
      nav.insertBefore(search, nav.firstElementChild);
    }
  }

  document.addEventListener('click', function (e) {
    if (Object(_utils_helpers__WEBPACK_IMPORTED_MODULE_0__["isOutSideClick"])(e, '.nav')) {
      var nav = document.querySelector('.nav');

      if (nav.querySelector('.nav__item.show')) {
        nav.querySelector('.nav__item.show').classList.remove('show');
      }
    }
  });
});

/***/ }),

/***/ "./app/js/modules/initPhotoSwipe.js":
/*!******************************************!*\
  !*** ./app/js/modules/initPhotoSwipe.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports) {

var initPhotoSwipeFromDOM = function initPhotoSwipeFromDOM(gallerySelector) {
  // parse slide data (url, title, size ...) from DOM elements
  // (children of gallerySelector)
  var parseThumbnailElements = function parseThumbnailElements(el) {
    var thumbElements = el.childNodes,
        numNodes = thumbElements.length,
        items = [],
        figureEl,
        linkEl,
        size,
        item;

    for (var i = 0; i < numNodes; i++) {
      figureEl = thumbElements[i]; // <figure> element
      // include only element nodes

      if (figureEl.nodeType !== 1) {
        continue;
      }

      linkEl = figureEl.children[0]; // <a> element

      size = linkEl.getAttribute('data-size').split('x'); // create slide object

      item = {
        src: linkEl.getAttribute('href'),
        w: parseInt(size[0], 10),
        h: parseInt(size[1], 10)
      };

      if (figureEl.children.length > 1) {
        // <figcaption> content
        item.title = figureEl.children[1].innerHTML;
      }

      if (linkEl.children.length > 0) {
        // <img> thumbnail element, retrieving thumbnail url
        item.msrc = linkEl.children[0].getAttribute('src');
      }

      item.el = figureEl; // save link to element for getThumbBoundsFn

      items.push(item);
    }

    return items;
  }; // find nearest parent element


  var closest = function closest(el, fn) {
    return el && (fn(el) ? el : closest(el.parentNode, fn));
  }; // triggers when user clicks on thumbnail


  var onThumbnailsClick = function onThumbnailsClick(e) {
    e = e || window.event;
    e.preventDefault ? e.preventDefault() : e.returnValue = false;
    var eTarget = e.target || e.srcElement; // find root element of slide

    var clickedListItem = closest(eTarget, function (el) {
      return el.tagName && el.tagName.toUpperCase() === 'FIGURE';
    });

    if (!clickedListItem) {
      return;
    } // find index of clicked item by looping through all child nodes
    // alternatively, you may define index via data- attribute


    var clickedGallery = clickedListItem.parentNode,
        childNodes = clickedListItem.parentNode.childNodes,
        numChildNodes = childNodes.length,
        nodeIndex = 0,
        index;

    for (var i = 0; i < numChildNodes; i++) {
      if (childNodes[i].nodeType !== 1) {
        continue;
      }

      if (childNodes[i] === clickedListItem) {
        index = nodeIndex;
        break;
      }

      nodeIndex++;
    }

    if (index >= 0) {
      // open PhotoSwipe if valid index found
      openPhotoSwipe(index, clickedGallery);
    }

    return false;
  }; // parse picture index and gallery index from URL (#&pid=1&gid=2)


  var photoswipeParseHash = function photoswipeParseHash() {
    var hash = window.location.hash.substring(1),
        params = {};

    if (hash.length < 5) {
      return params;
    }

    var vars = hash.split('&');

    for (var i = 0; i < vars.length; i++) {
      if (!vars[i]) {
        continue;
      }

      var pair = vars[i].split('=');

      if (pair.length < 2) {
        continue;
      }

      params[pair[0]] = pair[1];
    }

    if (params.gid) {
      params.gid = parseInt(params.gid, 10);
    }

    return params;
  };

  var openPhotoSwipe = function openPhotoSwipe(index, galleryElement, disableAnimation, fromURL) {
    var pswpElement = document.querySelectorAll('.pswp')[0],
        gallery,
        options,
        items;
    items = parseThumbnailElements(galleryElement); // define options (if needed)

    options = {
      // define gallery index (for URL)
      galleryUID: galleryElement.getAttribute('data-pswp-uid'),
      getThumbBoundsFn: function getThumbBoundsFn(index) {
        // See Options -> getThumbBoundsFn section of documentation for more info
        var thumbnail = items[index].el.getElementsByTagName('img')[0],
            // find thumbnail
        pageYScroll = window.pageYOffset || document.documentElement.scrollTop,
            rect = thumbnail.getBoundingClientRect();
        return {
          x: rect.left,
          y: rect.top + pageYScroll,
          w: rect.width
        };
      }
    }; // PhotoSwipe opened from URL

    if (fromURL) {
      if (options.galleryPIDs) {
        // parse real index when custom PIDs are used
        // http://photoswipe.com/documentation/faq.html#custom-pid-in-url
        for (var j = 0; j < items.length; j++) {
          if (items[j].pid == index) {
            options.index = j;
            break;
          }
        }
      } else {
        // in URL indexes start from 1
        options.index = parseInt(index, 10) - 1;
      }
    } else {
      options.index = parseInt(index, 10);
    } // exit if index not found


    if (isNaN(options.index)) {
      return;
    }

    if (disableAnimation) {
      options.showAnimationDuration = 0;
    } // Pass data to PhotoSwipe and initialize it


    gallery = new PhotoSwipe(pswpElement, PhotoSwipeUI_Default, items, options);
    gallery.init();
  }; // loop through all gallery elements and bind events


  var galleryElements = document.querySelectorAll(gallerySelector);

  for (var i = 0, l = galleryElements.length; i < l; i++) {
    galleryElements[i].setAttribute('data-pswp-uid', i + 1);
    galleryElements[i].onclick = onThumbnailsClick;
  } // Parse URL and open gallery if it contains #&pid=3&gid=1


  var hashData = photoswipeParseHash();

  if (hashData.pid && hashData.gid) {
    openPhotoSwipe(hashData.pid, galleryElements[hashData.gid - 1], true, true);
  }
}; // execute above function


initPhotoSwipeFromDOM('.my-gallery');

/***/ }),

/***/ "./app/js/modules/photoGallery.js":
/*!****************************************!*\
  !*** ./app/js/modules/photoGallery.js ***!
  \****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

// 1200 - container width
var sliderPhotosElem = document.querySelector('.js-photo-gallery');

if (sliderPhotosElem) {
  var sliderPhotos = new Splide(sliderPhotosElem, {
    perPage: 1,
    perMove: 1,
    gap: 65,
    pagination: false,
    lazyLoad: 'nearby',
    autoWidth: true,
    padding: {
      left: (window.screen.width - 1200) / 2 + 'px',
      right: (window.screen.width - 1200) / 2 + 'px'
    },
    breakpoints: {
      1240: {
        autoWidth: false,
        padding: {
          right: 220
        },
        gap: 50
      },
      991: {
        autoWidth: false,
        padding: {
          right: 165
        },
        gap: 30
      },
      639: {
        autoWidth: false,
        padding: {
          right: 80
        },
        gap: 15
      }
    }
  });
  sliderPhotos.mount();
  sliderPhotos.on('mounted', setCurrentIndexSLide(sliderPhotos));
  sliderPhotos.on('move', function (newIndex) {
    setCurrentIndexSLide(sliderPhotos, newIndex);
  });
}

/***/ }),

/***/ "./app/js/modules/productQuantity.js":
/*!*******************************************!*\
  !*** ./app/js/modules/productQuantity.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _createForOfIteratorHelper(o, allowArrayLike) { var it; if (typeof Symbol === "undefined" || o[Symbol.iterator] == null) { if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; var F = function F() {}; return { s: F, n: function n() { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }, e: function e(_e) { throw _e; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var normalCompletion = true, didErr = false, err; return { s: function s() { it = o[Symbol.iterator](); }, n: function n() { var step = it.next(); normalCompletion = step.done; return step; }, e: function e(_e2) { didErr = true; err = _e2; }, f: function f() { try { if (!normalCompletion && it["return"] != null) it["return"](); } finally { if (didErr) throw err; } } }; }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

// Product quantity
function incrementValue(e) {
  e.preventDefault();
  var target = e.target;
  var fieldName = target.dataset.field;
  var parent = target.parentNode;
  var currentVal = parseInt(parent.querySelector('input[name=' + fieldName + ']').value, 10);

  if (!isNaN(currentVal)) {
    parent.querySelector('input[name=' + fieldName + ']').value = currentVal + 1;
  } else {
    parent.querySelector('input[name=' + fieldName + ']').value = 1;
  }
}

function decrementValue(e) {
  e.preventDefault();
  var target = e.target;
  var fieldName = target.dataset.field;
  var parent = target.parentNode;
  var currentVal = parseInt(parent.querySelector('input[name=' + fieldName + ']').value, 10);

  if (!isNaN(currentVal) && currentVal > 1) {
    parent.querySelector('input[name=' + fieldName + ']').value = currentVal - 1;
  } else {
    parent.querySelector('input[name=' + fieldName + ']').value = 1;
  }
}

var fieldsQuantitys = document.querySelectorAll('.field-quantity');

if (fieldsQuantitys.length) {
  var _iterator = _createForOfIteratorHelper(fieldsQuantitys),
      _step;

  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var field = _step.value;
      field.addEventListener('click', function (event) {
        if (event.target.className === 'field-quantity__plus') {
          incrementValue(event);
        }

        if (event.target.className === 'field-quantity__minus') {
          decrementValue(event);
        }
      });
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
}

/***/ }),

/***/ "./app/js/modules/tabs.js":
/*!********************************!*\
  !*** ./app/js/modules/tabs.js ***!
  \********************************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _utils_helpers__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../utils/helpers */ "./app/js/utils/helpers.js");
 // tabs init

if (document.querySelector('[data-tabs]')) {
  tabbis();
}

document.addEventListener('tabbis', function (e) {
  var data = e.detail;
  setLabelActiveTab(data);
  var siblings = Object(_utils_helpers__WEBPACK_IMPORTED_MODULE_0__["getSiblings"])(data.pane);
  siblings.forEach(function (item) {
    item.classList.remove('show');
  });
  data.pane.classList.add('show');
}, false);
var tabsMobile = document.querySelectorAll('[data-tabs-mobile]');

if (tabsMobile) {
  tabsMobile.forEach(function (btn) {
    btn.textContent = btn.parentNode.querySelector('[tabindex="0"]').textContent;
    btn.addEventListener('click', function (e) {
      e.target.classList.toggle('active');
      var tabs = e.target.parentNode.querySelector('[data-tabs]');
      tabs.classList.toggle('show');
    });
  });
}

function setLabelActiveTab(data) {
  if (!data) {
    return false;
  }

  var parent = data.tab.closest('.tabs-nav');

  if (parent) {
    var mobileBtn = parent.querySelector('[data-tabs-mobile]') || null;

    if (mobileBtn) {
      mobileBtn.textContent = data.tab.textContent;
    }
  }
}

/***/ }),

/***/ "./app/js/utils/helpers.js":
/*!*********************************!*\
  !*** ./app/js/utils/helpers.js ***!
  \*********************************/
/*! exports provided: isHidden, isOutSideClick, getSiblings, onMountedHideArrowsSlider, overlayVisibility */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "isHidden", function() { return isHidden; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "isOutSideClick", function() { return isOutSideClick; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getSiblings", function() { return getSiblings; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "onMountedHideArrowsSlider", function() { return onMountedHideArrowsSlider; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "overlayVisibility", function() { return overlayVisibility; });
var isHidden = function isHidden(elem) {
  var style = window.getComputedStyle(elem);
  return style.display === 'none';
};

var getSiblings = function getSiblings(e) {
  var siblings = [];

  if (!e.parentNode) {
    return siblings;
  }

  var sibling = e.parentNode.firstChild; // collecting siblings

  while (sibling) {
    if (sibling.nodeType === 1 && sibling !== e) {
      siblings.push(sibling);
    }

    sibling = sibling.nextSibling;
  }

  return siblings;
};

var onMountedHideArrowsSlider = function onMountedHideArrowsSlider(slider) {
  if (slider.length <= slider.options.perPage) {
    slider.root.classList.add('arrows-hide');
    var arrows = slider.root.querySelector('.splide__arrows');
    arrows.style.display = 'none';
  }
};

var overlayVisibility = function overlayVisibility(display) {
  var overlay = document.querySelector('.overlay'),
      body = document.querySelector('body');

  if (overlay && display === true) {
    overlay.classList.add('show');
    body.classList.add('scroll-off');
  } else if (overlay && display === false) {
    overlay.classList.remove('show');
    body.classList.remove('scroll-off');
  }
};

var isOutSideClick = function isOutSideClick(event, selector) {
  var nodeElem = document.querySelector(selector);
  var isClickInsideElement = nodeElem.contains(event.target);
  if (!isClickInsideElement) return true;else return false;
};



/***/ }),

/***/ "./app/js/utils/setViewCurrentIndexSlide.js":
/*!**************************************************!*\
  !*** ./app/js/utils/setViewCurrentIndexSlide.js ***!
  \**************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony default export */ __webpack_exports__["default"] = (function (Splide, Components) {
  var elm;
  return {
    mount: function mount() {
      elm = document.createElement('span');
      var track = Components.Elements.track;
      var customArrows = Components.Elements.arrows;
      var nativeArrows = Components.Arrows.arrows;
      var prevArrow = nativeArrows.prev ? nativeArrows.prev : customArrows.prev;
      var arrowsParent = prevArrow.parentNode;
      arrowsParent.insertBefore(elm, prevArrow.nextSibling);
      this.setNumber(Splide.index + 1);
      this.bind();
    },
    bind: function bind() {
      var _this = this;

      Splide.on('move', function (newIndex) {
        _this.setNumber(newIndex + 1);
      });
    },
    setNumber: function setNumber(number) {
      var content = "".concat(number, "/").concat(Splide.length);

      if (elm.textContent !== undefined) {
        elm.textContent = content;
      } else {
        elm.innerText = content;
      }
    }
  };
});

/***/ })

/******/ });
//# sourceMappingURL=main.js.map